@model JobCategoriesViewModel

@{
    ViewData["Title"] = "Job Categories";
}

<div class="page-head">
    <div>
        <h1 class="page-title">Manage Job Categories</h1>
        <p class="subtitle">Add and manage job categories on the platform.</p>
    </div>
</div>

@if (TempData["Success"] != null)
{
    <div class="alert alert-success">@TempData["Success"]</div>
}
@if (TempData["Error"] != null)
{
    <div class="alert alert-danger">@TempData["Error"]</div>
}

<div class="panel">
    <h3 class="section-title">Add New Category</h3>

    <form asp-action="CreateJobCategory" method="post">
        @Html.AntiForgeryToken()

        <input type="hidden" name="pageNumber" value="@Model.PageNumber" />

        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

        <div class="form-group">
            <label>Category Name</label>
            <input type="text"
                   name="Name"
                   class="form-control"
                   placeholder="e.g. Software Development" />
        </div>

        <button type="submit" class="btn btn-primary">Add Category</button>
    </form>
</div>

@if (Model.Data == null || Model.Data.Count == 0)
{
    <div class="empty-state">
        <div class="empty-icon">&#127919;</div>
        <h3>No categories yet</h3>
        <p>No job categories registered.</p>
    </div>
}
else
{
    <div class="table-responsive">
        <table class="table">
            <thead>
                <tr>
                    <th>Category Name</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.Data)
                {
                    <tr>
                        <td>@item.Name</td>
                        <td>
                            <form asp-action="DeleteJobCategory" method="post" style="display: inline;" onsubmit="return confirm('Are you sure you want to delete this job category?');">
                                @Html.AntiForgeryToken()
                                <input type="hidden" name="jobCategoryId" value="@item.JobCategoryId" />
                                <input type="hidden" name="pageNumber" value="@Model.PageNumber" />
                                <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    @if (Model.TotalPageCount > 1)
    {
        <nav class="pager">
            @if (Model.PageNumber > 1)
            {
                <a class="pager-link" asp-action="JobCategories" asp-route-pageNumber="@(Model.PageNumber - 1)">&#8592; Previous</a>
            }

            <span class="pager-current">Page @Model.PageNumber of @Model.TotalPageCount</span>

            @if (Model.PageNumber < Model.TotalPageCount)
            {
                <a class="pager-link" asp-action="JobCategories" asp-route-pageNumber="@(Model.PageNumber + 1)">Next &#8594;</a>
            }
        </nav>
    }
}
